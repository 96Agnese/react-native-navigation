import {TextStyle} from 'react-native';

const defaultConfig = {
  regular: {
    fontFamily: 'RobotoMono-Regular',
  } as TextStyle,

  bold: {
    fontFamily: 'RobotoMono-Bold',
  } as TextStyle,
};

// const fontConfig: {ios: typeof defaultConfig; android: typeof defaultConfig} = {
//   ios: defaultConfig,
//   android: defaultConfig,
// };
export default defaultConfig;

// regular
// semibold
// bold
// black
